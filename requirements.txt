# inductiveBias: train / val / analysis / pretrained_val / bin/* import 기준
# 아래 버전은 제공하신 pip list와 맞췄습니다.
# torch/torchvision(+cu128)는 PyTorch CUDA 12.8 휠용 인덱스가 필요합니다.
#   CPU 전용이면: https://pytorch.org 에서 맞는 pip 한 줄로 대체하세요.

--extra-index-url https://download.pytorch.org/whl/cu128

# --- pip list에서 고정 (핵심 스택) ---
numpy==2.4.1
PyYAML==6.0.3
tqdm==4.67.1
pillow==12.1.0
torch==2.10.0+cu128
torchvision==0.25.0+cu128
# 예전에 torchaudio만 낮은 버전이 남아 있으면 torch와 충돌 경고가 남 — 짝 맞추거나 미사용 시 제거
torchaudio==2.10.0+cu128
wandb==0.25.0
timm==1.0.25
huggingface_hub==1.2.3

# 분석·시각화·기하 스크립트
matplotlib==3.10.8
pandas==3.0.1
seaborn==0.13.2
einops==0.3.0
pyhessian==0.1

# 목록에 없었지만 코드에서 cv2 사용 (bin/geometry 등)
opencv-python>=4.8.0
